# ── Vision backend (choose one) ─────────────────────────────────────────────

# Option A: OpenAI (gpt-4o) — best accuracy
OPENAI_API_KEY=sk-...
OPENAI_MODEL=gpt-4o

# Option B: Ollama (local, free) — run `ollama pull llava` first
# USE_OLLAMA=true
# OLLAMA_URL=http://host.docker.internal:11434   # use this when running in Docker
# OLLAMA_MODEL=llava

# ── Storage ──────────────────────────────────────────────────────────────────
DB_PATH=/data/shelfscan.db
UPLOAD_DIR=/uploads
